# Frontend API targets
# Set these in Render for the client/static service.
# If omitted in local dev, the app now falls back to http://127.0.0.1:5000.

VITE_API_BASE_URL=
VITE_EARNINGS_API_BASE_URL=
